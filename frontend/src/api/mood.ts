import {request} from '../utils/request';import type {Mood,MoodTag,MoodTrashItem} from '../types';export const listMoods=(date?:string)=>request<Mood[]>(`/moods${date?`?date=${date}`:''}`);export const saveMood=(payload:{mood_level:number;mood_tags:MoodTag[];note:string;record_date:string})=>request<Mood>('/moods',{method:'POST',body:JSON.stringify(payload)});export const updateMood=(id:number,payload:{mood_level:number;mood_tags:MoodTag[];note:string;record_date:string})=>request<Mood>(`/moods/${id}`,{method:'PUT',body:JSON.stringify(payload)});export const deleteMood=(id:number)=>request<{deleted:number;message:string}>(`/moods/${id}`,{method:'DELETE'});export const listTrashMoods=()=>request<MoodTrashItem[]>('/moods/trash');export const restoreMood=(id:number)=>request<Mood>(`/moods/${id}/restore`,{method:'POST'});
