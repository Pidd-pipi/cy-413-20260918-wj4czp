import {Button,Card,Col,DatePicker,Form,Input,Popconfirm,Row,Slider,Space,Typography,message} from 'antd';import dayjs from 'dayjs';import {useEffect,useState} from 'react';import {useNavigate} from 'react-router-dom';import {deleteMood,listMoods,saveMood} from '../api/mood';import {MoodCard} from '../components/common/MoodCard';import {MoodSelector} from '../components/common/MoodSelector';import {EmptyState} from '../components/common/EmptyState';import type {Mood,MoodTag} from '../types';export function Moods(){const [moods,setMoods]=useState<Mood[]>([]),[tags,setTags]=useState<MoodTag[]>(['happy']),[date,setDate]=useState<string>();const nav=useNavigate();const load=()=>listMoods(date).then(setMoods).catch(e=>message.error(e.message));useEffect(()=>{load()},[date]);const remove=async(id:number)=>{try{const r=await deleteMood(id);setMoods(prev=>prev.filter(m=>m.id!==id));message.success(r.message||'已移入回收站');load()}catch(e){message.error((e as Error).message)}};return <><Typography.Title>情绪记录</Typography.Title><Card className="filter-bar"><DatePicker placeholder="按日期筛选" onChange={v=>setDate(v?.format('YYYY-MM-DD'))}/><Button onClick={()=>setDate(undefined)}>清除筛选</Button><Button onClick={()=>nav('/trash')}>回收站</Button></Card><Row gutter={[20,20]}><Col xs={24} lg={9}><Card title="新增一条记录"><Form layout="vertical" initialValues={{level:6,date:dayjs()}} onFinish={async v=>{try{await saveMood({mood_level:v.level,mood_tags:tags,note:v.note||'',record_date:v.date.format('YYYY-MM-DD')});message.success('已保存');load()}catch(e){message.error((e as Error).message)}}}><Form.Item name="date" label="日期"><DatePicker style={{width:'100%'}}/></Form.Item><Form.Item name="level" label="心情指数"><Slider min={1} max={10}/></Form.Item><Form.Item label="情绪标签"><MoodSelector value={tags} onChange={setTags}/></Form.Item><Form.Item name="note" label="备注"><Input.TextArea rows={3}/></Form.Item><Button type="primary" htmlType="submit">保存记录</Button></Form></Card></Col><Col xs={24} lg={15}><Card title={`记录列表 (${moods.length})`}>{moods.length?<div className="card-list">{moods.map(m=><div key={m.id}><MoodCard mood={m}/><Space style={{marginTop:8}}><Popconfirm title="移除这条记录？" description="七天内可在回收站按原日期还原" onConfirm={()=>remove(m.id)}><Button danger size="small">移除</Button></Popconfirm></Space></div>)}</div>:<EmptyState title="还没有符合条件的情绪记录"/>}</Card></Col></Row></>}
